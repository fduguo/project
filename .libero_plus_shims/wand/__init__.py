"""Small Wand compatibility layer for LIBERO-plus evaluation.

This shim is only intended for the local LIBERO-plus motion-blur path, where
the Python `wand` package would otherwise require a system ImageMagick install.
"""

